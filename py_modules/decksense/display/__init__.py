"""Display Studio module.

Owns the gamescope filter wrapper and the preset catalog. Future layout:
- ``adapters/``  — gamescope command runner.
- ``services/``  — apply / revert preset, confirmation timer.
- ``domain.py``  — Preset model, parameter ranges.
"""
